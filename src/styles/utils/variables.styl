// Paths
$base = '/'

$paths = {
  base: base
  images: pathjoin( $base, 'images/' )
  fonts: pathjoin( $base, 'fonts/' )
}

// Fonts
fontFace( 'AktivGrotesk', $paths.fonts + 'aktivgrotesk-medium' )

$font-text--primary = 'AktivGrotesk'

$WHITE = #f7f7f7
$BLACK = #000000

$OVERALL_MARGIN = 20px